#ifndef QTZ_AUTHENTICATOR_H
#define QTZ_AUTHENTICATOR_H


#include "identity.hpp"
#include "token.hpp"

class Authenticator {
public:
//    virtual Authenticator()=0;
//    virtual bool authenticate(const Identity* id, const Token* token) const=0;
};

#endif // QTZ_AUTHENTICATOR_H
